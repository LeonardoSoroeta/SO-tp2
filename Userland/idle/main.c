#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>
#include <libc.h>

int main(unsigned int pcount, char * pgname[], void * pgptrs[]) {

	while(1) {
		// putc('h');
		halt();
	}

	return 0;
}
